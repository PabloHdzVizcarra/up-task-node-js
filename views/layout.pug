<!DOCTYPE html>
html(lang="es")
  head
    include partials/header
  body
    header.main-title
      h1 Up Tasks - Administrador de proyectos
      a(href="/cerrar-sesion" class="main-title-btnClose") Cerrar sesion

    main.container
      aside.container-projects
        .panel-create-project
          a(href="/nuevo-proyecto" class="btn") Nuevo Proyecto
        .panel-list-projects
            h2(class="list-projects-title") Proyectos
            ul#projects.projects
              if (proyectos.length)
                each proyecto in proyectos
                  li
                    a(href=`/proyectos/${proyecto.url}`) #{proyecto.name}
      block contenido

      script(src="/dist/bundle.js")

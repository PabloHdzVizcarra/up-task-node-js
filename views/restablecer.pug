<!DOCTYPE html>
html(lang="es")
  head
    include partials/header
  body(class='body-color')
    div(class='container-form')
      h1(class='container-form__title') #{namePage}

      if locals.messages
        div(class='alerts')
          each category in Object.keys(locals.messages)
            each error in messages[category]
              div(class=`alerts__alert ${category}`)
                p #{error}

      form(method='POST' action='/restablecer' class='container-form__form')
        p(class='container-form__info') Agrega tu E-mail para restablecer el password
        div(class='container-form__form__field')
          label(for="email") E-mail:
          input(
            class='container-form__form__field__input'
            type="email" 
            name='email' 
            placeholder='Tu Email'
            value= email
          )
        div(class='container-form__form__field-send')
          input(
            type="submit" 
            class='container-form__form__field__input-submit' 
            value='Restablecer Password'
          )
        div(class='container-form__form__actions')
          a(
            href="/iniciar-sesion"
            class='container-form__form__actions-register'
          ) Ya lo recorde, volver a iniciar sesion


  script(src="/dist/bundle.js")
